import React, { Component } from 'react';
import CheckinHistory from './checkin_history'

class Dashboard extends Component {
    render(){
        return (
            <CheckinHistory />
        );
    }
}

export default Dashboard;